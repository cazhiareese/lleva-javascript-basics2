import math from './math.js'
// the import can be named anything

console.log(math(5, 2)); // note that we referred to it as “math” since that was the name used in the import. But it can be anything